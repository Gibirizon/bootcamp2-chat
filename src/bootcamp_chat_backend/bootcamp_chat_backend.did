type UserData = record {
    nickname : text;
    avatar_rul: opt text;

}
service : {
    add_chat_msg : (text, principal) -> ();
    get_chat : (vec principal) -> (opt vec text) query;
    register : (text) -> ();
    get_user : (principal) -> (opt UserData) query;
    get_users : () -> (vec record {principal; UserData}) query;
}
